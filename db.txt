mysql -u root -e "CREATE USER wordpress_user@'%.%.%.%' IDENTIFIED BY 'password'"
mysql -u root -e "CREATE DATABASE wordpress;use wordpress"
mysql -u root -e "use wordpress;GRANT ALL PRIVILEGES ON * TO wordpress_user@'%.%.%.%' WITH GRANT OPTION; FLUSH PRIVILEGES;"
